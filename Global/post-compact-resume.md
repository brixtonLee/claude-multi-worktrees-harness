POST-COMPACT RESUME: Read docs/active-context.md then docs/current-plan.md. You ARE the orchestrator — do NOT spawn an orchestrator sub-agent. If Last Verdict is PENDING_VERIFICATION, spawn VERIFIER via Task(subagent_type=verifier). If REWORK, spawn CODER fix via Task(subagent_type=coder). Otherwise resume from first unchecked subtask by spawning CODER sub-agents via Task tool. SHELL RULES: NEVER use pipes (|), &&, ||, or ; in Bash commands. Run each command as a separate Bash call. Redirections (2>/dev/null) are OK.
